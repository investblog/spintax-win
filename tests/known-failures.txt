# Known-failing corpus cases.
#
# EMPTY. The whole golden corpus passes: PASS=168 FAIL=0 SKIP=4, the 4 skips being
# kind:rng render cases, which assert within-engine reproducibility rather than a
# cross-engine output and are engine-private by design.
#
# It was not always empty. The cosmetic post-process stage was originally ported in a
# minimal form and 21 render-postprocess cases failed; they are listed in the history of
# this file. The stage is now a full port of the reference's 12-step pipeline.
#
# check-corpus.sh diffs the runner's actual failures against this file, so:
#   * ANY failure is now a regression and blocks the push;
#   * if a case ever legitimately becomes expected-to-fail, add it here WITH a reason --
#     an empty baseline is a claim, and it should not be weakened silently.
#
# Format: <fixture file><space><space><case id>. Sorted. Comments and blank lines ignored.
